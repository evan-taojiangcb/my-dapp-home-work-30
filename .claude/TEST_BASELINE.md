# TEST_BASELINE.md

## 现有测试目录

```
tests/
├── unit/
│   ├── web/       # （空，尚未填充）
│   ├── server/    # （空，尚未填充）
│   └── packages/  # （空，尚未填充）
├── e2e/           # （空，尚未填充）
└── reports/
    ├── chrome/     # （空，Chrome DevTools MCP 截图留存目录）
    └── webmcp/     # （空，WebMCP 交互报告目录）
```

**现状**：测试目录结构已由 `sdlc-init` 创建，但所有测试文件均为空，尚未实现。

## 现有 Lint / Typecheck / Unit / E2E / Browser 验收入口

| 类别 | 命令 | 入口 | 状态 |
|------|------|------|------|
| Lint | `pnpm lint`（turbo run lint） | 各 package.json | **未配置** lint 脚本（eslint/biome 均未在 root 或各 app 配置） |
| Typecheck | `pnpm check-types` | turbo 统一调用 `tsc -b` | **已配置** — turbo.json 中 `check-types` task |
| Unit | `jest` / `vitest` | `tests/unit/` | **未实现** — 框架未安装（.env 中声明 `TEST_FRAMEWORK=jest`） |
| E2E | `playwright` | `tests/e2e/` | **未实现** — Playwright 未安装配置 |
| Browser 交互验收 | Chrome DevTools MCP / WebMCP | 浏览器自动化 | **未验证** — Chrome DevTools MCP 连接状态未知 |

## 当前是否已具备 Chrome DevTools MCP / WebMCP 最终交互验收能力

| 能力 | 状态 | 说明 |
|------|------|------|
| Chrome DevTools MCP | **未验证** | MCP server 配置存在（chrome-devtools-mcp skill 可用），需在 VSCode 中启用并验证连接 |
| WebMCP | **未验证** | WebMCP 依赖 Chrome DevTools MCP，需先确认 CDP 连接可用 |
| Playwright 预检 | **未实现** | `tests/e2e/` 为空，无 playwright.config.ts |

## 验收能力缺口列表

| 缺口 | 优先级 | 建议命令 |
|------|--------|---------|
| ESLint 配置 | 高 | 在各 app/package 中初始化 eslint config |
| Jest/Vitest 单元测试框架 | 高 | `pnpm add -Dw jest @types/jest ts-jest` 或 `pnpm add -Dw vitest` |
| Playwright E2E 配置 | 中 | `pnpm add -Dw @playwright/test && npx playwright install chromium` |
| `tests/unit/web/` 首个测试文件 | 中 | 建议从 `apps/web/src/utils/trpc.ts` 的 client 初始化测试入手 |
| Chrome DevTools MCP 连接验证 | 中 | 使用 `chrome-devtools-mcp` skill 验证 CDP 连接到 localhost:9222 |
| Browser 自动化验收脚本 | 低 | 待 MCP 连接验证后，基于 `tests/e2e/` 编写 Playwright + CDP 联动测试 |

## .env 当前测试配置

```
TEST_FRAMEWORK=jest        # 声明使用 jest（未安装）
E2E_FRAMEWORK=playwright   # 声明使用 playwright（未安装）
TEST_BOOTSTRAP_POLICY=auto # 自动补齐（sdlc-doit 阶段可触发）
REVIEW_MAX_ROUNDS=3        # 设计/代码审查最大轮数
```

## 建议的结构保护相关测试

进入 SDLC 工作流后，以下边界应作为回归测试目标：

1. `packages/api/src/routers/todo.ts` — tRPC router 不得被删除或重命名
2. `packages/db/src/schema/` — Drizzle schema 不得迁移至其他 ORM
3. `apps/web/src/utils/trpc.ts` — tRPC client 初始化方式不得变更
4. `packages/env/src/server.ts` + `packages/env/src/web.ts` — env 验证分离模式不得合并

---
*Generated by SDLC Workflow sdlc-init (existing project intake)*
