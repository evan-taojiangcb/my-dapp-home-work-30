# PROJECT_BASELINE.md

## 项目根路径
`/Volumes/HS-SSD-1TB/works/my-dapp-home-work-30`

## 包管理器 / Workspace / 构建系统

| 字段 | 值 |
|------|-----|
| 包管理器 | pnpm 10.30.3 |
| Workspace 格式 | pnpm-workspace.yaml（catalog 分区） |
| 构建系统 | Turbo 2.8.12 |
| 构建工具 | tsdown（server）、Next.js 16（web/fumadocs） |
| 节点要求 | 未在根 package.json 声明（隐式 modern） |

### Catalog 分区依赖
```
dotenv: ^17.2.2
zod: ^4.1.13
"@cloudflare/workers-types": ^4.20251213.0
hono: ^4.8.2
"@trpc/server": ^11.13.4
alchemy: ^0.90.0
drizzle-orm: ^0.45.1
react: ^19.2.3
react-dom: ^19.2.3
tailwindcss: ^4.1.18
"@tanstack/react-query": ^5.90.12
```

## 核心技术栈

| 层 | 技术选型 |
|----|---------|
| Web 前端 | Next.js 16（App Router）、React 19、Tailwind CSS v4 |
| 后端 | Hono（@hono/node-server）、tRPC 11 |
| 数据库 | Drizzle ORM + SQLite（drizzle-kit） |
| Web3 | @solana/web3.js、@solana/wallet-adapter（Phantom） |
| RPC | Alchemy（Ethereum）、Solana Devnet |
| 部署 | Cloudflare Workers（@opennextjs/cloudflare）、Wrangler、Alchemy |
| 类型 | TypeScript 5 |
| UI 组件 | Radix UI、Sonner、Lucide React |
| Monorepo | Turbo 2 + pnpm catalogs |

## 当前可识别的运行脚本

### 根级（turbo）
```bash
pnpm dev              # turbo dev — 启动所有 dev 任务
pnpm build            # turbo build — 全量构建
pnpm check-types      # turbo check-types — 全量类型检查
pnpm dev:web          # turbo -F web dev
pnpm dev:server       # turbo -F server dev
pnpm db:push          # turbo -F @my-dapp-home-work-30/db db:push
pnpm db:studio        # turbo -F @my-dapp-home-work-30/db db:studio
pnpm db:generate      # turbo -F @my-dapp-home-work-30/db db:generate
pnpm db:migrate       # turbo -F @my-dapp-home-work-30/db db:migrate
pnpm deploy           # turbo -F @my-dapp-home-work-30/infra deploy
pnpm destroy          # turbo -F @my-dapp-home-work-30/infra destroy
```

### 各 workspace
| Workspace | 关键脚本 |
|-----------|---------|
| web | `next dev`、`next build` |
| server | `tsx watch src/index.ts`、`tsdown build`、`bun build --compile` |
| fumadocs | `next dev --port=4000`、`fumadocs-mdx` |
| infra | `alchemy dev`、`alchemy deploy` |
| ui | `check-types` |

## 外部依赖

| 服务 | 用途 | 配置位置 |
|------|------|---------|
| Alchemy | Ethereum RPC（Mainnet + Sepolia） | `NEXT_PUBLIC_ALCHEMY_API_KEY` |
| WalletConnect | Ethereum WalletConnect 协议 | `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` |
| Phantom Wallet | Solana 钱包 | 前端 SDK（无 API Key） |
| Solana Devnet | Solana 测试网络 | 无需 API Key |
| Cloudflare | Workers 部署 | Wrangler 配置 |

## Verified Facts

- [x] Monorepo 结构已锁定：`apps/*` + `packages/*`，根级无 `src/` 业务代码
- [x] 所有 apps 通过 pnpm workspace 引用内部包（`@my-dapp-home-work-30/*`）
- [x] `packages/config` 作为 tsconfig base 被所有包依赖
- [x] `packages/env` 导出 `server.ts` 和 `web.ts` 两套 env 验证配置
- [x] `packages/api` 是 tRPC 路由定义层，供 server 和 web 共同使用
- [x] `packages/db` 负责 schema 和 drizzle ORM 封装
- [x] Solana 钱包集成：Phantom adapter + @solana/web3.js，已配置 devnet 支持
- [x] Ethereum 钱包集成：WalletConnect + Alchemy RPC
- [x] Next.js App Router，web 和 fumadocs 使用独立 Next 实例
- [x] `.env` 已配置 TG_USERNAME、`NEXT_PUBLIC_*` 等环境变量

## Claimed but Unverified

- [ ] `pnpm deploy` / `pnpm destroy` 实际执行结果（infra 包结构待确认）
- [ ] Alchemy 和 WalletConnect API Key 的实际可用性
- [ ] Drizzle DB 迁移脚本是否已完整迁移到生产
- [ ] `apps/server` 生产环境启动方式（Hono + node 或 bun）
- [ ] Phantom wallet 连接在真实浏览器扩展下的完整流程

## Intake 结论

本项目为 **existing project**，已具备完整的 monorepo 工程结构、钱包集成和 Web3 RPC 配置。后续需求必须基于本 baseline 进行评估，任何偏离以下结构的技术方案（如新增根目录业务目录、替换 ORM/ RPC 层）必须经 `design.md` 明确批准。

---
*Generated by SDLC Workflow sdlc-init (existing project intake)*
