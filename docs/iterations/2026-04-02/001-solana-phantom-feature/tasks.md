# Tasks — Solana Phantom Wallet Integration

## 基本信息

| 字段 | 值 |
|------|-----|
| 迭代目录 | docs/iterations/2026-04-02/001-solana-phantom-feature/ |
| 总任务数 | 8 |
| 预估工时 | ~2h |

---

## T-001: 修复 layout.tsx 中的 broken import

**文件**: `apps/web/src/app/layout.tsx`

**描述**: 当前 `layout.tsx` 引用了已删除的 `@/components/providers-dynamic`，需替换为新的动态 Provider 路径。

**验收标准**:
- [x] `layout.tsx` 不再引用不存在的 `@/components/providers-dynamic`
- [x] `layout.tsx` 导入 `ProvidersDynamic` 从正确路径 `@/components/wallet/providers-dynamic`

---

## T-002: 创建目录结构和 `providers-dynamic.tsx`

**文件**: `apps/web/src/components/wallet/providers-dynamic.tsx`

**描述**: 创建 `components/wallet/` 目录结构和动态加载的 Provider 组件。使用 `next/dynamic` + `ssr: false` 解决 WalletAdapter 的 SSR 兼容问题。

**验收标准**:
- [x] `providers-dynamic.tsx` 使用 `dynamic(() => import(...), { ssr: false })`
- [x] 导出默认 `ProvidersDynamic` 组件接受 `children` prop

---

## T-003: 创建 `solana/SolanaProvider.tsx`

**文件**: `apps/web/src/components/wallet/solana/SolanaProvider.tsx`

**描述**: 创建 Solana Provider 封装，包含 `ConnectionProvider` + `WalletProvider(PhantomWalletAdapter)` + `WalletModalProvider`。网络固定为 Devnet，开启 `autoConnect`。

**验收标准**:
- [x] `ConnectionProvider` endpoint 为 `https://api.devnet.solana.com`
- [x] `WalletProvider` 使用 `PhantomWalletAdapter`，`autoConnect={true}`
- [x] 引入 `@solana/wallet-adapter-react-ui/styles.css`

---

## T-004: 创建 `solana/SolanaConnectButton.tsx`

**文件**: `apps/web/src/components/wallet/solana/SolanaConnectButton.tsx`

**描述**: 封装 `WalletMultiButton`，作为用户触发钱包连接的入口按钮。

**验收标准**:
- [x] 导出默认组件使用 `<PhantomButton />`（带自定义 labels）
- [x] 引入 `@solana/wallet-adapter-react-ui/styles.css`

---

## T-005: 创建 `solana/useSolanaBalance.ts` hook

**文件**: `apps/web/src/components/wallet/solana/useSolanaBalance.ts`

**描述**: 使用 `@tanstack/react-query` + `@solana/web3.js` 的 `Connection.getBalance()` 实现 SOL 余额查询。失败返回 `null`。

**验收标准**:
- [x] 使用 `useQuery` 且 `enabled: !!publicKey`
- [x] 余额以 SOL 为单位（除以 `LAMPORTS_PER_SOL`）
- [x] 网络错误返回 `null` 而非抛出异常

---

## T-006: 创建 `solana/WalletInfoPanel.tsx`

**文件**: `apps/web/src/components/wallet/solana/WalletInfoPanel.tsx`

**描述**: 展示已连接钱包的地址（可点击复制）、网络（Solana Devnet）、余额（带加载状态）。包含断开连接按钮。

**验收标准**:
- [x] `connected=false` 时返回 `null`（不渲染）
- [x] 地址显示前4+后4位，中间 `...`，点击复制完整地址
- [x] 网络显示 "Solana Devnet"
- [x] 余额显示 4 位小数，`isLoading` 时显示 "Loading..."
- [x] 断开按钮调用 `disconnect()`

---

## T-007: 创建 `solana/index.ts` 导出文件

**文件**: `apps/web/src/components/wallet/solana/index.ts`

**描述**: 统一导出所有 Solana wallet 相关组件和 hook。

**验收标准**:
- [x] 导出 `SolanaConnectButton`、`WalletInfoPanel`、`SolanaProvider`、`useSolanaBalance`

---

## T-008: 修改 `page.tsx` 集成钱包面板

**文件**: `apps/web/src/app/page.tsx`

**描述**: 在首页中集成钱包连接按钮和信息面板。钱包 UI 应在页面主内容区展示。

**验收标准**:
- [x] `SolanaConnectButton` 渲染并可见
- [x] 连接成功后 `WalletInfoPanel` 自动出现
- [x] 页面无 TypeScript 错误

---

## 任务执行顺序

```
T-001 → T-002 → T-003 → T-004 → T-005 → T-006 → T-007 → T-008
```

> T-002 需先完成（创建目录），T-003~T-006 可并行开发，T-007 依赖 T-003~T-006，T-008 最后集成。

---

## 验收总览

| 任务 | 验收标准数 | 对应 AC |
|------|-----------|---------|
| T-001 | 2 | — |
| T-002 | 2 | — |
| T-003 | 3 | AC-001, AC-007 |
| T-004 | 2 | AC-001 |
| T-005 | 3 | AC-005 |
| T-006 | 5 | AC-002, AC-003, AC-004, AC-005, AC-006 |
| T-007 | 1 | — |
| T-008 | 3 | AC-001, AC-002 |

---
*Generated by SDLC Workflow sdlc-doit — Step ④ task-generator*
