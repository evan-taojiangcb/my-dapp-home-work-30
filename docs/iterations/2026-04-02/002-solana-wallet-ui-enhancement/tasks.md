# Tasks — Solana/Ethereum Dual-Chain Wallet UI Enhancement

## 基本信息

| 字段 | 值 |
|------|-----|
| 迭代目录 | docs/iterations/2026-04-02/002-solana-wallet-ui-enhancement/ |
| 总任务数 | 6 |
| 预估工时 | ~1.5h |

---

## T-001: 创建 `NetworkProvider.tsx` + `useNetworkContext`

**文件**: `apps/web/src/components/wallet/solana/NetworkProvider.tsx`

**描述**: 创建 `NetworkProvider` 和 `useNetworkContext`，管理当前选中的网络 ID（`solana-devnet` | `ethereum-sepolia`）。Provider 包裹在 `SolanaProvider` 外层，暴露 `networkId`、`endpoint`、`isSepolia`。

**验收标准**:
- [ ] `NetworkProvider` 接受 `children` prop
- [ ] `useNetworkContext()` 返回 `{ networkId, endpoint, isSepolia }`
- [ ] `endpoint` 为 `https://api.devnet.solana.com`（当 `networkId === "solana-devnet"`）
- [ ] Sepolia 时 `isSepolia === true`，`endpoint === ""`

---

## T-002: 创建 `NetworkSelector.tsx` 下拉组件

**文件**: `apps/web/src/components/wallet/solana/NetworkSelector.tsx`

**描述**: 使用 HTML `<select>` 实现网络选择下拉。包含 Solana Devnet 和 Ethereum Sepolia 两个选项。Sepolia 选中时触发 toast 提示"请安装 Ethereum 钱包"。

**依赖**: T-001（需要 `useNetworkContext`）

**验收标准**:
- [ ] 下拉显示 "Solana Devnet (SOL)" 和 "Ethereum Sepolia (ETH)"
- [ ] 默认选中 Solana Devnet
- [ ] 选择 Sepolia 时 toast 提示用户需要 Ethereum 钱包（使用 `sonner`）
- [ ] 选择 Solana Devnet 时调用 `setNetworkId`

---

## T-003: 重构 `Header.tsx` — 添加钱包按钮和网络选择器

**文件**: `apps/web/src/components/header.tsx`

**描述**: 将 `SolanaConnectButton` 和 `NetworkSelector` 集成到 Header，水平横向排列。

**验收标准**:
- [ ] Header 使用 `flex items-center gap-3` 水平排列子元素
- [ ] 右侧依次显示 `<SolanaConnectButton />` 和 `<NetworkSelector />`
- [ ] Header 在 `layout.tsx` 中仍然被引用

---

## T-004: 更新 `providers-dynamic.tsx` — 包裹 `NetworkProvider`

**文件**: `apps/web/src/components/wallet/providers-dynamic.tsx`

**描述**: 在 `SolanaProvider` 外层包裹 `NetworkProvider`，使网络上下文对所有钱包组件可用。

**验收标准**:
- [ ] `NetworkProvider` 在 `SolanaProvider` 外部被渲染
- [ ] `QueryClientProvider` 在最外层（不变）

---

## T-005: 更新 `WalletInfoPanel.tsx` — 响应式网络切换 + 余额联动

**文件**: `apps/web/src/components/wallet/solana/WalletInfoPanel.tsx`

**描述**:
1. 引入 `useNetworkContext`，在 Sepolia 模式下显示 "— ETH" 而非 SOL 余额
2. 确保 `publicKey` 变化时，地址/余额立即响应（已有架构，不改逻辑）

**验收标准**:
- [ ] Sepolia 模式下余额显示 "— ETH"
- [ ] Solana Devnet 模式下显示 SOL 余额（不变）
- [ ] `isSepolia` 为 false 时，Solana 余额正常显示

---

## T-006: 更新 `page.tsx` — 统一钱包卡片样式

**文件**: `apps/web/src/app/page.tsx`

**描述**: 将钱包 section 的外层样式从 `rounded-lg border p-4 space-y-4` 改为 `rounded-xl border bg-card p-4 space-y-3`，与 `WalletInfoPanel` 内部卡样式一致。

**验收标准**:
- [ ] 钱包 section 使用 `rounded-xl border bg-card p-4 space-y-3`
- [ ] 移除独立的 SolanaConnectButton（已移至 Header）

---

## 任务执行顺序

```
T-001 → T-002 → T-003 → T-004 → T-005 → T-006
```

> T-001 和 T-002 可并行开发（都是新文件）。T-003 依赖 T-002。T-004 依赖 T-001。T-005 依赖 T-001。T-006 最后执行。

---

## 验收总览

| 任务 | 验收标准数 | 对应 AC |
|------|-----------|---------|
| T-001 | 4 | AC-002, AC-003 |
| T-002 | 4 | AC-002, AC-004 |
| T-003 | 3 | AC-001 |
| T-004 | 2 | AC-003 |
| T-005 | 3 | AC-003, AC-005 |
| T-006 | 2 | AC-005 |

---
*Generated by SDLC Workflow sdlc-doit — Step ④ task-generator*
